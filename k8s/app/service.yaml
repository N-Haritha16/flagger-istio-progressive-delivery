apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: progressive-delivery
  labels:
    app: demo-app
spec:
  selector:
    app: demo-app
  ports:
    - name: http
      port: 80
      targetPort: 8080

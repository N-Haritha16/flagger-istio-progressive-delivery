apiVersion: flagger.app/v1beta1
kind: Canary
metadata:
  name: demo-app-canary-failure
  namespace: progressive-delivery
spec:
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: demo-app-v2
  service:
    port: 80
    name: demo-app
    gateways:
      - demo-gateway
    hosts:
      - "*"
  analysis:
    interval: 30s
    threshold: 3
    maxWeight: 50
    stepWeight: 10
    metrics:
      - name: request-success-rate
        thresholdRange:
          min: 99
        interval: 30s
      - name: request-duration
        thresholdRange:
          max: 500
        interval: 30s
    webhooks:
      - name: load-test
        url: http://flagger-loadtester.progressive-delivery/
        timeout: 5s
        metadata:
          cmd: "hey -z 25s -q 10 -c 5 http://demo-app.progressive-delivery/"
